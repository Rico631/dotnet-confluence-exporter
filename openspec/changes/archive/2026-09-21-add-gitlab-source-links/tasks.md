# Tasks

## 1. Модели и параметры рендера

- [x] 1.1 Добавить в `HandlerDocumentation` относительный путь исходного файла handler и проверить, что модель сохраняет значение при создании документации
- [x] 1.2 Добавить в `MethodDocumentation` относительный путь исходного файла метода и проверить, что вложенные узлы call tree сохраняют собственные пути
- [x] 1.3 Добавить в `HandlerDocumentationRenderOptions` GitLab reference со значением по умолчанию `main` и проверить обратную совместимость существующего вызова `Render`

## 2. Сбор относительных путей

- [x] 2.1 Вычислять путь handler относительно корня Git-репозитория и передавать его в `HandlerDocumentation`; проверить результат на handler из вложенного каталога
- [x] 2.2 Вычислять и сохранять путь `SyntaxTree` для каждого метода `CallGraphNode` при преобразовании в `MethodDocumentation`; проверить call tree с методами из разных файлов
- [x] 2.3 Обработать отсутствие Git-репозитория или исходного location без исключения и проверить, что соответствующий путь остаётся пустым

## 3. GitLab-ссылки в renderer

- [x] 3.1 Сформировать URL файла GitLab в формате `/-/blob/{reference}/{relative-path}` с нормализацией разделителей и проверить ссылку на handler
- [x] 3.2 Обернуть имя handler и имя каждого метода call tree в ссылку только при наличии repository URL и относительного пути; проверить fallback для неполных данных
- [x] 3.3 Реализовать безопасное кодирование reference и сегментов пути с сохранением `/` и HTML-кодированием `href`; проверить вложенные каталоги и специальные символы
- [x] 3.4 Сохранить существующие commit-ссылки и проверить, что изменение file links не меняет формат `/-/commit/{hash}`

## 4. Проверка результата

- [x] 4.1 Добавить или обновить тесты моделей, анализа путей и `HandlerDocumentationRenderer` для сценариев из `code-documentation-source-links`; проверить их успешное выполнение
- [x] 4.2 Собрать решение командой `dotnet build WebApp4.slnx` и убедиться, что новые ссылки корректно рендерятся в XHTML Confluence